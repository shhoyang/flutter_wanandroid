import 'package:flutter/material.dart';

/// @Author: Yang Shihao
/// @Date: 2021-01-06

abstract class HRadius {
  static const BorderRadius r4 = BorderRadius.all(Radius.circular(4.0));
  static const BorderRadius r6 = BorderRadius.all(Radius.circular(6.0));
  static const BorderRadius r32 = BorderRadius.all(Radius.circular(32.0));
  static const BorderRadius r50 = BorderRadius.all(Radius.circular(50.0));
}
